import 'package:integration_test/integration_test_driver.dart';

// Lets `flutter drive --use-application-binary` run integration tests from a
// prebuilt APK (see docs/testing.md).
Future<void> main() => integrationDriver();
